name: signals_store
description: A reactive signals store.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  signals_store_annotation:
    path: ../signals_store_annotation

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
