name: signals_store_annotation
description: Annotations for signals_store code generation.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
