name: signals_store_annotation
description: >-
  Annotations for signals_store code generation. Marks an abstract class as a
  reactive store description and drives the signals_store_generator.
version: 0.3.1
repository: https://github.com/bysensa/signals-store
issue_tracker: https://github.com/bysensa/signals-store/issues
topics:
  - signals
  - state-management
  - codegen

environment:
  sdk: ^3.12.0

resolution: workspace

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
