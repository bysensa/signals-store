name: signals_store_example
description: End-to-end example and smoke test for signals_store_generator.
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

resolution: workspace

dependencies:
  signals: ^7.0.0
  signals_store_annotation:
    path: ../signals_store_annotation
  signals_store_generator:
    path: ../signals_store_generator

dev_dependencies:
  build_runner: ^2.4.0
  lints: ^5.0.0
  test: ^1.25.0
