name: signals_store_generator
description: Code generator for signals_store.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  signals_store_annotation:
    path: ../signals_store_annotation
  analyzer: ^8.1.1
  build: ^4.0.0
  source_gen: ^4.2.0

dev_dependencies:
  build_test: ^3.0.0
  lints: ^5.0.0
  test: ^1.25.0
